# Windows troubleshooting
For Windows Wi-Fi problems, determine whether the adapter is detected and connected, then separate IP connectivity from DNS. Driver or network-reset operations should be recommended only after simpler observations and should require user confirmation before changes.
