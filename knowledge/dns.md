# DNS
DNS translates domain names to IP addresses. If a public IP is reachable but domain lookup fails, DNS is a likely problem. Use DNS lookup as an observation before changing resolver configuration. State-changing network fixes require user confirmation.
