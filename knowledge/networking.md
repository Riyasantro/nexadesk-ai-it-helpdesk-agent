# Networking
For Wi-Fi connected but no internet, distinguish interface, IP connectivity, gateway, and DNS. Check interface state first. Then ping a known public IP. Then test DNS. If IP connectivity works but DNS fails, investigate DNS. A failed ping alone does not prove the internet is down because ICMP may be blocked.
